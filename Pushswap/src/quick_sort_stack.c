#include "push_swap.h"


int quic_sort_stack(t_list **root, t_list **tail, t_list **root2, t_list **tail2)
{
    int index;

    if ((*tail)->index > 2)
    {
        index=(*tail)->index;
        while (index/2)
        {
            index--;
        }
    }
    return 0;
}